import { validationResponses } from '../data/validationResponses'

export const validationService = {
  getAll: async () => validationResponses,
}

// Extension points: Feishu Forms, Feishu Base exports, and a real database.
